import Vue from 'vue';
import Counter from "../counter/Counter.vue";

const App = Vue.extend({
    components:{
        Counter
    },
    data(){
        return {

        }
    }
});
export default App;
